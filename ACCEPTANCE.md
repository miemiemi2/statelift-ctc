# 最小验收场景

## 场景 A：正常付款

创建 G 和 E1，锁定 R 与足额 B。执行者在 T 前完成付款。认证成功后，赢家获得 R，用户不能再次提取 R，B 回到可用余额。

## 场景 B：付款已发生但证明迟到

E1 在源端已经完成付款，但在 D 前没有完成认证。D 后用户可取回 R1。迟到证明到达后，只能从 B1 支付 E1 赢家；不得从 R1 支付，不得再次付款。

## 场景 C：付款人失联，安全接力

E1 未完成付款并到期。用户取得 R1 退款权；创建沿用同一 G 的 E2。E2 可完成一次付款并结算。之后任何 E1 迟到事实都不能造成第二次源链付款或第二次赢家奖励。

## 必须验证的边界

- D 前后边界严格按链上受理时间判断，不能由前端时间决定。
- 证明进入但尚未完成结算，不算已清账。
- R、B、赢家奖励、保障费分别记账，不能混用或重复计算。
- 同一 G、同一 E、同一成功事实重放都必须失败。
- 保障资本不足时不能创建“已受保”报价。
- 测试替身必须明确标注，不得称为真实 Attestcoin proof。

## 保上限验收

- README 或产品入口必须用一个具体付款运营者场景说明：预算冻结造成什么损失、保障费买到什么结果。
- 示例报价必须同时显示 R、B、T、D、保障费、覆盖范围和最坏责任；所有数字标为演示参数。
- 必须说明为什么“自己留备用金”“普通 solver 重试”“单独退款工具”不能同时得到安全接力、有限期限预算恢复和迟到责任归属。
- 三幕操作完成后，第一次使用者能说出自己购买了什么，而不是只说系统验证了什么。
- 如果实现为了简单而删掉购买结果、把 B 变成凭空无限资金、或把产品退化为普通退款/重试，则阶段失败，即使所有单元测试通过。
