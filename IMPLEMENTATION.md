# StateLift 实现层

## 当前目标：保上限，补下限

做出一个可运行的最小产品，证明“跨链付款超时保障”这一竞争主张：同一笔付款结果不明时，用户不会重复付款，也不会无限冻结预算；付款人失联可以换人，迟到的旧付款由该轮专属保障资金承担。

实现必须同时满足两道门：

- **补下限**：状态机、资金归属、重试、防重复和失败路径真实可运行，不能是静态 UI 或作者自证。
- **保上限**：成品仍然像一个付款运营者愿意购买的服务，而不是普通退款、重试或证明看板。必须保留明确用户损失、保障费、担保责任和相对现成 solver 的差异。

本阶段只做本地 EVM 纵向切片，不接真实 Attestcoin 测试网，不接真实钱包，不做多资产、通用平台、SP1 批量证明或生产部署。

## 不可缩水的用户承诺

1. 同一业务目标 `G` 最多发生一次合规源链付款，所有重试沿用同一个 `G`。
2. 每一轮报价 `E` 有独立预算 `R`、专属足额保障资金 `B`、源付款截止 `T`、清账截止 `D`。
3. D 前完成并认证的付款，从该轮 R 结算；B 释放。
4. 到 D 仍未清账的轮次，R 归用户；不能靠调用顺序抢走退款权。
5. D 后才证明旧付款成功，只能从该轮 B 支付实际赢家，不能动用已归还的 R。
6. G 已经付款后，任何新执行者都不能造成第二次源链付款。
7. 证明缺失不等于付款未发生；状态机必须区分未付款、已付款未认证、已认证、已退款和迟到保障结算。

## 购买理由（示例，不得冒充市场事实）

目标用户是管理跨链付款预算的运营者。付款结果迟迟未确认时，预算会被占用，运营者既不能安全重试，也不能安全退回预算。产品以透明的 R、B、T、D 和保障费报价出售“有限期限的预算恢复＋安全接力”。所有费率、容量和担保金额均标为演示参数，不能声称已有客户或现成流动性。

实现不能删除或弱化以下购买判断：运营者承担的冻结成本是什么；为什么自己留备用金或普通 solver 不等价；保障方承担的最坏损失是什么；报价何时不应成立。数字可以是明确标注的示例，但不能完全留给文案阶段。

## 阶段出口

只有以下全部成立，才可进入真实测试网阶段：本地三幕测试通过；状态和资金不变量有明确断言；不带源码说明的人能按 README 跑通；失败路径没有静态 UI 冒充链上结果；`evidence/` 保存原始测试输出和状态转移记录；购买场景、示例报价和与普通 solver 的差异仍能在成品中被看懂。任一门未通过，都不得接真实测试网。
