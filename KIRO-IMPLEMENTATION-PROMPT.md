# Kiro 启动 Prompt

将下面内容直接粘贴到 Kiro CLI。模型由用户在 CLI 中选择 `claude-opus-5`。

```text
你现在负责 StateLift 的前两个实现阶段。先读取当前目录中的 IMPLEMENTATION.md、ACCEPTANCE.md、STATUS.md、AGENTS.md 和 README.md。

不要读取或恢复项目外已归档的旧候选方案。不要接入真实 Attestcoin 测试网，不要使用真实钱包，不要要求主网资金。当前只做本地 EVM 纵向切片。

本任务有两道必须同时通过的门：

第一道是“补下限”：状态机、资金归属、退款、接力、防重复和失败路径真实可运行，不能是静态 UI、作者自证或把 mock 当真实协议。

第二道是“保上限”：成品必须仍然像一个付款运营者愿意购买的服务，而不是普通退款、重试或证明看板。具体用户必须能看懂自己支付保障费后买到什么，保障方承担什么，为什么普通 solver、自己留备用金或单独退款工具不等价。

核心竞争主张只有这一条：

跨链付款结果不明时，用户不会无限冻结预算，也不会重复付款；付款人失联可以换人，迟到的旧付款由该轮专属保障资金承担。

先做“价值锁定”，再写代码：

1. 在 IMPLEMENTATION.md 或独立的 PRODUCT-SLICE.md 中写出一个具体付款运营者场景：预算是多少、冻结多久、造成什么运营损失。
2. 写出一张透明的演示报价：R（用户预算）、B（专属担保）、T（源付款截止）、D（清账截止）、保障费、覆盖范围和最坏责任。所有数字必须明确标成演示参数，不能冒充市场报价。
3. 用一段短文字说明为什么以下替代不能同时给出本产品的完整结果：自己留备用金、普通 solver 重试、单独退款工具。
4. 任何后续工程取舍都必须检查是否损害这项购买理由。为了简单而删除购买所得、把担保变成无限资金、或把产品退化为普通退款/重试，必须停止并报告，不得自行缩水。

第一阶段：本地最小纵向切片

先检查现有 StateLift 代码，复用有价值的证明、Escrow、CLI 和测试基础；不要因为已有代码存在就假设组合版已经实现。实现一个最小可运行状态机，至少覆盖：

- 创建业务目标 G；
- 创建 E1，锁定 R1 和足额 B1；
- E1 正常付款并完成认证；
- E1 已付款但认证迟到；
- E1 未付款并到期；
- 沿同一 G 创建 E2；
- E2 完成付款；
- 迟到的 E1 事实到达；
- R、B、赢家奖励和保障费分开记账；
- 同一付款目标、同一轮次和同一成功事实重放失败；
- D 前后边界按链上受理时间判断；证明进入但尚未完成结算不算清账；保障资本不足不能创建受保报价。

优先做合约和可验证状态，不要先做复杂 UI。需要测试替身时，保持接口与未来真实 Attestcoin 接入清晰分离，并在输出中明确标注。

为 ACCEPTANCE.md 的场景 A、B、C 增加测试和资金不变量断言。每次修改后运行相关测试，把原始输出和状态转移记录保存到 evidence/，更新 STATUS.md。不要把“代码写完”或“单元测试通过”写成产品完成。

第一阶段的出口必须同时满足：

- 三幕本地测试全部通过；
- 状态和资金不变量有明确断言；
- 购买场景、报价、保障责任和替代比较仍在 README 或入口中可理解；
- 不带源码说明的人能按 README 跑通；
- evidence/ 保存原始证据；
- 没有把 mock、synthetic proof 或静态页面报告成真实 Attestcoin/testnet 集成。

第二阶段：不带源码带路的完整操作验收

第一阶段通过后，开一个新的操作上下文或至少停止查看源码，只依据 README、CLI 帮助或产品入口操作。像第一次使用的付款运营者一样验证：

1. 正常付款；
2. 付款已发生但证明迟到，D 后用户取回 R，迟到证明由 B 结算；
3. 付款人失联，用户取回 R，第二个执行者沿同一 G 接手；
4. 旧付款最终被证明成功时，系统仍只付款一次；
5. 重放、错误轮次、错误目标、D 边界和保障资本不足都失败；
6. 操作者能说清：自己支付了什么、获得什么、保障方承担什么、为什么普通 solver 或自己留备用金不等价；
7. 入口没有把“预算已归还”“旧付款待证明”“第二轮可接手”“最终只付款一次”混成一个模糊状态。

第二阶段不要为了让验收通过而修改承诺或隐藏失败。把失败记录到 evidence/，区分产品行为错误、文档/入口错误、测试替身限制和未实现的真实协议能力。

完成两个阶段后：

- 更新 STATUS.md，明确写出已完成、未完成和阻塞；
- 生成 IMPLEMENTATION-REPORT.md；
- 列出进入真实 Attestcoin 测试网前必须解决的事项；
- 不要自行接入真实测试网；
- 不要部署主网；
- 不要声称已经满足比赛的真实 testnet 集成要求。

工作原则：保持一条从用户输入到最终结果的连续纵向链路；普通成熟实现直接采用；只对会改变核心承诺、购买理由或成本结构的未知进行比较；如果实现方向与 IMPLEMENTATION.md 的任一核心门冲突，先指出冲突并停下来，不自行缩水。
```
