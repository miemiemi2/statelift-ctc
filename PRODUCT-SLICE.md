# StateLift 购买场景与演示报价（阶段 1 冻结稿）

本文件在写代码之前冻结「用户买到什么」。所有数字都是**演示参数**，用于本地纵向切片和验收断言；不是市场报价、不是已有客户、不是已备流动性。任何后续工程取舍都必须回到本文件检查是否损害购买理由。

---

## 1. 具体付款运营者场景

**用户**：一个在 Creditcoin 上用 CTC 管理付款预算的结算运营方（供应商结算 / 赏金 / grant 发放）。它必须让 Ethereum 上一个固定收款地址收到**确定数量的原生 USDC**，收款人不接受跨链、换币或新钱包。

**这一笔具体付款**：向收款人支付 `5,000 USDC`（演示参数）。运营方为这一笔锁定预算 `R = 5,150`（本地链原生单位，演示参数；含付给实际赢家的全部报酬）。

**出问题的样子**（这是产品要卖的痛点，不是已测量的行业统计）：执行者已经在 Ethereum 上付了款，但源链最终性、取证、Attestcoin 认证或提交环节任一处卡住，Creditcoin 侧迟迟拿不到「已付款」的事实。此时运营方同时失去两件东西：

1. **预算使用权**：`R` 仍锁在这一笔上。运营方不敢退款——万一钱已经付出去了，退款就是白送。
2. **继续付款的能力**：运营方不敢换人重试——万一旧付款其实成功了，重试就是**第二次真实付款**，5,000 USDC 有去无回。

**造成的运营损失（示例假设，非测量值）**：本演示把冻结窗口设为最坏 `24 小时`。在此期间该笔预算不可用于下一轮付款，运营方要么排队等待、要么临时追加一份预算，并且必须人工对账才能判断到底付没付。冻结成本 = 追加预算的资金占用 + 人工判断成本 + 付款排期延后。**注意：真实运营方是否愿意为这个损失付费，本阶段没有验证。**

---

## 2. 演示报价（全部为演示参数）

运营方在开单（`openRound`）时看到并接受下面这张表。所有字段都在链上写入该轮 `E`，不可修改。

| 字段 | 演示值 | 含义 |
|---|---|---|
| 付款目标 `G` | 收款人 + `5,000 USDC` + 业务唯一编号 | 长期不变。所有重试沿用同一个 `G` |
| `R` 用户预算 | `5,150` 单位 | 该轮报价上限，含赢家全部报酬。**演示参数** |
| `B` 专属担保 | `5,150` 单位（`B ≥ R`） | 保障方为**这一轮**锁定的资金，不得同时担保其他未决轮次 |
| `T` 源付款截止 | 开单 + `1,800` 秒 | 源链合规付款必须在此之前发生 |
| `D` 清账截止 | 开单 + `7,200` 秒（= `T + 5,400`） | 到此仍未完成链上结算，`R` 的归属固定为用户 |
| `π` 保障费 | `31` 单位（≈ `R` 的 `0.6%`）| 开单时支付给保障方。**不随退款返还** |
| 覆盖范围 | 「该轮已合规付款，但认证未能在 `D` 前完成结算」 | 赔付上限 `R`，一次 |
| 保障方最坏责任 | `= R = 5,150` 单位 | 收款人已收到 USDC，用户同时取回 `R`，差额由保障方实付 |

**报价何时不应成立**（写进合约校验，不只是文案）：

- `B < R` → 拒绝开单。担保不足额时不存在「受保报价」。
- 保障方可用资本 `< B` → 拒绝开单。不允许凭空承保。
- `D ≤ T` 或 `D - T` 小于约定窗口 → 拒绝开单。否则参与者可以等 `D` 后退款、再在仍有效的 `T` 内正常付款，必然消耗担保金，完全不需要出现任何故障。

**明确不覆盖**：`R` 的美元购买力；Creditcoin 停机时的墙钟准时提款；已花掉的源链 gas；`π` 本身；以及任何人向收款地址主动赠款（保证范围只是同一 `G` 下的合规执行）。

**诚实的代价**：极端情况下收款人确实收到了 USDC，用户又确实取回了 `R`，保障方实际净损失 `R`。这是**收费、预先锁足资金的清账期限责任**，不是无风险垫款。本产品不再承诺「实际付款与用户预算返回绝不同时发生」。保留的强约束是：同一 `G` 最多一次合规付款、实际赢家最多获偿一次、每一句承诺都有明确资金来源。

---

## 3. 为什么以下替代不能同时给出同一个结果

产品所得由三件事**同时**构成，缺一件就退化：

- **A. 安全接力** = 源链上每个 `G` 只有一次性闸门，换人不可能造成第二次付款；
- **B. 有限期限预算恢复** = 到 `D` 由链上受理时间自动固定 `R` 归用户，不取决于谁抢先发交易、也不取决于后台是否愿意；
- **C. 迟到责任归属** = 每轮有专属足额 `B`，退款之后到达的旧履约仍有钱可赔。

| 替代方案 | 有 A？ | 有 B？ | 有 C？ | 缺的是什么 |
|---|---|---|---|---|
| 自己多留一份备用金 | ✗ | 部分 | ✗ | 备用金解决的是「手上有没有钱」，不解决「敢不敢再付一次」。运营方自己没有跨轮的源链一次性闸门，换人重试仍可能真实付两次；旧付款迟到时也没有第三方担责，损失全归自己 |
| 普通 solver / intent 重试 | 部分 | ✗ | ✗ | 目标链防重通常只保护「同一个订单」，跨轮换人的防重与退款资格留在中心化订单库；预算什么时候能退由后台裁量，没有链上到 `D` 的自动归属 |
| 单独的超时退款工具 | ✗ | ✓ | ✗ | 能到期退钱，但退款后旧付款若被证明成功，赢家没有资金来源。结果要么运营方被迫二次付款，要么执行者白干——所以运营方仍然不敢在退款后接力 |
| **StateLift 组合版** | ✓ | ✓ | ✓ | — |

一句话版本：**别的方案让你「拿回钱」或者「换个人」，但没有一个让你在不知道旧付款到底成没成的情况下，安全地同时做这两件事。**

---

## 4. 不可缩水清单（工程取舍的红线）

出现以下任一情况，即使全部单元测试通过，阶段也算失败，必须停下来报告：

1. 为简化而删掉购买所得，或把产品退化为普通退款 / 普通重试；
2. 把 `B` 变成凭空的无限资金，或放宽 `B ≥ R`、放宽「保障资本不足不得开单」；
3. 把 `D` 的归属改为依赖调用顺序 / 抢跑；
4. 把测试替身、synthetic proof 或静态页面当作真实 Attestcoin / testnet 集成来报告。
